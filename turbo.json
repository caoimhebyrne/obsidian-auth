{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "outputs": ["dist/**"]
        },
        "build-watch": {
            "persistent": true,
            "cache": false
        }
    }
}
